name: iOS Checks

on:
  workflow_call:

jobs:
  ios-checks:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - name: Print start message
        run: |
          echo "Starting iOS checks..."
