name: iOS Checks
description: Run any iOS specific checks for linting, testing, etc.

runs:
  using: composite
  steps:
    - name: Build and test
      runs-on: ubuntu-latest
      shell: bash
      run: |
        echo "Starting iOS checks..."